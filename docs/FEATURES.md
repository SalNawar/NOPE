# NOPE — Feature Inventory

The contract of what the game does. **Any change that removes or alters a
listed behaviour must update this file in the same commit.** Tested = covered
by the EditMode suite in `Assets/Tests/EditMode`.

## Run & meta

- [ ] Title scene → new run / continue via `RunManager` (+ `SaveSystem` persistence: a save is written whole to a temp file and swapped in atomically, the old save kept as a backup, and a load that finds the save missing or unreadable recovers the run from the temp file, else the backup (which file tested: `SaveFilesTests`); New Run deletes all three; a player keeps the slot in its persistent data folder, the editor in the project's `Library/EditorSaves`, so each worktree's editor has its own save and parallel plays never overwrite one another's (the rule tested: `SaveLocationTests`); save version 2 — version-1 saves from the made-up world are ignored, Title offers New Run; a version-2 save from before history loads with an empty history; the News site's back issues (`newsArchive`), the Mail read flags (`mailRead`), the clerk's statement (`accountDays`) and the Notes pages (`notes`) are additive too: an older save loads them empty); Continue resumes where the save was made: at Home after the end-of-shift save (the finished shift is never replayed), otherwise in the Office (a save without the resume point, from before history, resumes in the Office) (checked in Unity); Continue re-emits the present culture's cue from the saved history, idempotently, so an older save themes too
- [ ] Day plans per day number from `ContentLibrary_Main`; a day without its own plan reuses the latest earlier plan (the inspector plan only when the library has none) (plan choice tested: `DayPlansTests`). Ramp: day 1 Ancient × Egypt/Iraq/Greece/Italy (8); day 2 + Medieval, + China/Britain, no Ancient Egypt (10); day 3 + Early modern, all 8 countries, no Medieval China / Early modern Japan (12); day 4 + Industrial, no Victorian Britain (12); day 5 + Modern, no Weimar Berlin / New Kingdom Egypt (13); day 6 + the Future (its leader's place, only while a nation leads), no Showa Tokyo (14); days 7+ replay day 6's plan; days 4-6 keep day 3's tell count, channels (dress tells included) and premade pool. Each day weighs its traveller kinds (`world_source.json` `days[].kinds`, written into `DayPlanSO` kinds; the blueprint pick is one weighted draw on the case stream, so the kind costs no new draw, and a premade's slot draws only the displaced (tested: `TravellerKindsTests`)): day 1 is rich tourists beside the displaced (1 : 1), days 2-6 the displaced only, until the redesign's phases 8 and 12 settle the mix. Each day weighs its traveller kinds (`world_source.json` `days[].kinds`, written into `DayPlanSO` kinds; the blueprint pick is one weighted draw on the case stream, so the kind costs no new draw, and a premade's slot draws only the displaced (tested: `TravellerKindsTests`)): day 1 is rich tourists beside the displaced (1 : 1), days 2-6 the displaced only, until the redesign's phases 8 and 12 settle the mix
- [ ] Deterministic case generation: same run + same day + same history (the timeline leader and the latched fact edits, see History) + the premades already met = same travellers, and history draws no random number; one seeded stream per traveller (`Seeds.ForCase`), plus a day stream for rule violators, a per-traveller lie stream (`Seeds.ForLies`), a per-traveller dialog stream (`Seeds.ForDialog`, small-talk pick), a per-traveller look stream (`Seeds.ForLooks`: gender when unknown, skin, face, hair colour), a per-slot premade stream (`Seeds.ForLegendary`: the roll and the pick) and a per-traveller account stream (`Seeds.ForAccount`: a displaced person's Displacement No., unique within the day, the day they were found, 1 to 30 days before today, which is also their incident's day, their incident number and their certificate's Valid Until, 3 to 365 days after today; `AgencyNumbers`, its ranges in `world_source.json` `agency.displaced`; tested: `AgencyNumbersTests`; a 2150 citizen's Citizen Account instead, in a fixed order: the Citizen ID, the debt, the transponder model and serial, the lineage, the past trips, then one honest Valid Until per expiring form, every number unique within the day and never the clerk's own Citizen ID; `AccountMaker`, its ranges in `agency.accounts` and `agency.transponders`; order, ranges, uniqueness and pinned numbers tested: `AccountMakerTests`), a per-traveller forms seed (`Seeds.ForForms`: a value, never a stream, that each paper's serial comes from, "TC-610/489711": the form number and six digits of the seed and the paper's index; serials tested: `FormSerialsTests`), and at Home a night stream for the slot machine's spins (`Seeds.ForSlot`); a premade met on an earlier day changes which slots hold premades and so, through the day's name roster, later names and dress tells; which of today's questions may carry a spoken tell depends only on the day (and the interview wiring), so buying Interview Protocols or setting a story flag never changes the tell options (who lies still depends on the liar chance, which includes the slot-machine modifier and forgery-risk effects, see World); day seed formula unchanged, every stream's seeds pinned (seeding tested: `SeedsTests`, `SeededRandomTests`, `WeightedRandomTests`; tell eligibility tested: `InterviewDayTests`; whole-day determinism is checked in Unity, not by the EditMode suite); desk interactions (papers, scans, the wheel) draw no random numbers and never change generation
- [ ] Endings (`EndingService`): fired (firing threshold on stability) and bankrupt (bankruptcy threshold) are checked after every verdict, at the end of a shift that applied dialog consequences, and at sleep (the debug Skip Day sleeps too); Retirement (day 15) and the attribute epilogues only at sleep, so the run ends after day 15 is played, and an epilogue whose attribute total is reached (Art 41, Science 26, Democracy 9: the 65th percentile of perfect play's day-15 totals over 50 simulated runs; ties go to Democracy, Science, Art) replaces Retirement (each condition, at or below the firing and bankruptcy lines, from the day or the attribute total, and the selection tested: `EndingRulesTests`; the checks' timing is checked in Unity)
- [ ] Home phase between days: expenses, family conditions & care (the medical drain counts each member's condition, never below 0; care takes one point off for the care cost when the wallet covers it; each night a member may worsen by one, up to the cap, on a roll seeded by the day and their place in the family: tested: `HomeRulesTests`), slot machine (pay-rate modifier; a night's spins draw in turn from the run's own stream for that day, `Seeds.ForSlot`, so a run replays and Continue cannot reroll a spin: stream tested: `SeedsTests`), upgrades (the shop shows 6 at a time with a "Page n/m" row whose "Next >" turns the page, wrapping round; buying keeps the page; today's 8 upgrades, the four Speech translators among them (see Translation), fill 2 pages; a discounted price is the listed cost less the active discount, rounded half to even, the one rule for the price shown (tested: `ShopPricesTests`; the price shown equals the price charged, checked in Unity)) (page maths tested: `PagingTests`); Home's household costs (the day's expenses and any care paid) and purchases (upgrades bought and slot spins paid), with the wallet after, are written into the day's row of the clerk's statement as they happen (see Citizen Account); a Home panel wired without its continue button is skipped, so the evening never strands (checked in Unity); the family and shop rows are written in their panel's own ink (the panel's body text colour, dark on the art's cream panels), and the HUD (day, wallet, stability) sits on a dark strip across the top of the room's art

## Office scene — the art office

- [ ] The office is the art side's 3D room (`Assets/Scenes/OfficeScene.unity`, the active scene: its lighting, cameras, desk, PC and props) with the game on top: `Assets/Scenes/OfficeGameplay.unity` (built by Build Office UI) loads additively whenever the art office loads (Title, Home, or Play on either scene); the art scene's leftover gameplay objects are switched off at load and the game never edits it; the game finds the art's places through the scene contract (an `Anchor_{id}` object, else a named art object, else a default pose; logged at load; `Tools > TimeDesk > Check Office Scene Contract` reports them and `Add Gameplay Anchors` is for the art side; names and resolution order tested: `OfficeContractTests`; `docs/SCENE_CONTRACT_GAMEPLAY.md`); the traveller, a layered figure standing behind the desk turned to the camera, appears when READY is tapped and leaves at the decision
- [ ] 3D input: every clickable is a box on the `Interactable` layer placed over its art at load; the office camera's PhysicsRaycaster sees only that layer (the art's own colliders take no clicks). Click the PC → the PC frame: a front-facing monitor frame over the office, left of centre with the room visible on its right (placeholder bezel), the live desktop in its glass (a camera draws the desktop into the glass's rectangle), a red close X, a power button and LED; close it with the X, a click on the room beside it, Escape or the taskbar "< Desk" button; while it is open only the desktop and the frame take input, and papers held in the hand, which sit beside it with their rows clickable (a hole in its click-outside area; a click elsewhere outside the frame still closes it) (input table and the frame-open invariant tested: `BoothRulesTests`; the glass viewport tested: `ScreenMappingTests`)
- [ ] Live monitor: the office PC's screen shows the same desktop (4:3, 1440×1080 units), cloned live into a texture projected onto the art's glass (the glass is the renderer or material named Glass or Screen; letterboxed; fit tested: `ScreenMappingTests`), and keeps running while you work at the desk; between travellers it reads "Waiting for the next traveller" (on a strip, so it never reads off the wallpaper)
- [ ] Screen power: the PC's power knob, the frame's power button or Start ▸ Turn off screen darkens the screen (the office PC shows its own dark glass, the frame a dark screen); the PC keeps running (scans reach the PC, the clock ticks); a new traveller or a finished scan wakes it (`DeskConfigSO` toggles); while a citation slip waits for Acknowledge the screen stays on and cannot be turned off (power, wake and hold rules tested: `PcScreenTests`; the power buttons' citation row tested: `BoothRulesTests`)
- [ ] Diegetic readouts on the art's own texts: the day calendar (today's date in the agency's calendar and the day number, "14 MAR 2150 · DAY 1" (UI string `desk.calendar`): day 1 is the agency block's first date, 14 Mar 2150, and each shift day the next calendar day, written as birth dates are (tested: `AgencyCalendarTests`); formatted when the day changes, never per frame; its text wraps and shrinks to fit the art's box, which was drawn for a day number; the plain "Day n" without a readable first date), the stability monitor (percent, amber below 60 and red below 30), the credits till (with ding) and the digital shift clock; the game writes the NEXT sign's caption (UI string `desk.readyCaption`); an art office without those texts gets a small fallback HUD; clicking the calendar ("Today: 14 MAR 2150 · DAY 1"), stability monitor, till or clock shows its value as a tooltip (the templates are UI string keys in `DeskReactionSO`; the till's names the wallet's word)
- [ ] Timeline-reactive poster (sprite swaps driven by timeline cues)
- [ ] Morning briefing as "THE TEMPORAL TIMES" newsletter over the office (Start Shift) (the office ignores clicks while a newsletter is up; tested: `BoothRulesTests`); when it shows, the day's paper is kept as that day's back issue for the Internet's News site (`WorldState.newsArchive`, day order, at most `DesktopConfigSO` 30 issues, the oldest dropped; a replayed day replaces its issue) (tested: `NewsArchiveTests`)
- [ ] End-of-day "SHIFT LEDGER" newsletter over the office (Go Home), incl. the undocumented-denials line ("log a deviation before denying") (the office ignores clicks while a newsletter is up; tested: `BoothRulesTests`)
- [ ] Per-case READY gate: visitor is presented only after the player taps the NEXT sign (tested: `ReadyGateTests`)
- [ ] The art's digital clock display shows the shift clock (the analog placeholder clock is retired)
- [ ] The art's background crowds (`OfficeHallCrowds`) turn from their morning to their evening colours with the shift clock, whatever the queue: morning until half the shift (13:00), fully evening from 90% (16:12), eased between; the art office without the gameplay layer keeps them in the morning. The art reads the gameplay's read-only hook (`IShiftProgress`, `ShiftClockDriver.Live`) (the curve and its clock times tested: `CrowdPaletteBlendTests`; the crowds on screen are checked in Unity)

## Desk

- [ ] Papers: a traveller hands each document over once, their kind's primary form when they step up (the displaced's TC-610 Displacement Certificate) and the others when requested (`DocumentTemplateSO` hand-over); papers are lit sheets lying on the 3D desk that slide in from the traveller's side and print their document's form (redesign phase 4): a Temporal Customs agency form laid out by the one form engine (`FormLayout`, from the template's `form` and the form style `FormStyle_Agency`), the same form the scanned copy will show: the header (the agency line "TEMPORAL CUSTOMS" and its programme line "Debt Relief Departures" from the agency block, the title in capitals with its form number at the right, and a faint agency seal behind), numbered section bars, each field in a box on a 12-column grid (its label in bold small capitals over its value; a value keeps its size on one line, else shrinks to the floor, where it may wrap to two lines, and its box grows), the primary form's 4:5 head-and-shoulders photo in a cell beside the first two boxes (the other forms have none), a form's paragraph, and the foot: a rule, the issuing line, the barcode drawn from the serial with the serial beside it, the dashed stamp area captioned FOR OFFICIAL USE · DESK STAMP, and fine print; the displaced's TC-610 (Full Name beside the photo, Displacement No., Date of Birth and Valid Until; then Origin and Incident), TC-620 (Declarant and Displacement No.; Coin of Home and Native Tongue; Effects Carried; the declaration taken down in English) and TC-630 (Returnee and Displacement No.; Return To; Incident and Departure; the return line); a rich tourist's TC-101 (Full Name beside the photo, Citizen ID, Date of Birth and Valid Until; then Destination and Visa Class) and TC-230 (Citizen ID and Departure; Transponder and Transponder Class; Currency Carried and Declared Effects), each one page, the fit checked like the displaced's at the accounts' widest values (a Citizen ID, a status, a transponder class, a debt; `FieldLengths`, tested: `FieldLengthsTests`); a field's page is the page its form places it on (layout, reading order, spans, the photo cell, value fitting, capitals, pages, flow pages and the hit test tested: `FormLayoutTests`; the barcode tested: `BarcodeTests`; the rows and the page from the form tested: `DocumentRowsTests`); every value is in English, as filled, like the scanned copy's (documents are never in a tongue, see Translation); a click on a paper lifts it into the hand, in front of the camera: two slots low beside the screen's centre, the side it lay on unless that slot would hide another paper on the desk and the other would not; a third sends back the one held longest; a handed-over paper lands where it shows whole on the screen: the next spawn spot in turn, else the first of the other spawn spots and then of a 5 × 4 grid of spots over the mat (nearest its centre first, never on the scanner; `DeskConfigSO.landingGrid`) that neither a held paper's place (its slot, raised or dipped under the wheel), the case HUD's strips, the speech bubble nor the wheel's ring covers; when none does (both slots full on a 16:9 screen), the paper held longest goes back where it lay, as a third pick sends it back, and the paper lands clear of the one still held and of the returned one; else where it shows most (the landing rule tested: `PaperLandingTests`); right-click, a click on it off its boxes (the header, the photo, a gutter, the foot), a click on the desk or Escape puts it back where it lay, on top of the stack; a held paper is evenly lit (unlit while held) and readable from 1280×720 to 2560×1080 (held at 0.44 screen heights, a value is 15.5 px and a label 12 px at 720p, the floors of 13 and 12 px met, each at least 4.5:1 on its box as drawn), and the box under the pointer tints; dragging a held paper drops it back onto the desk under the pointer and on, e.g. to the scanner, but only while the papers on the desk take input: beside the open PC frame a held paper takes clicks, while a press-and-move on it starts no drag, so it stays held and the release counts as a click (when the drag-out is live tested: `BoothRulesTests`); a drag cut short (the paper's input taken away mid-drag) sends the paper back to where it was picked up; while the wheel is open held papers dip below it (holds, slots, the paper held longest, click routing and the cover rule tested: `DeskPapersTests`; the pose and layouts tested: `ExamineLayoutTests`); the click no longer brings a paper to the top; drag them anywhere on the desk (dragged across the desk plane, the centre kept on the desk; they stay where dropped, on top: papers stack by height, the dragged one lifted above them); a sliding or scanning paper cannot be picked up (hand-over and which states can be dragged tested: `DeskPapersTests`; the desk area and stacking tested: `DeskGeometryTests`)
- [ ] Desk scanner (a placeholder flatbed on a free spot of the desk until the art adds one): drop a paper on it to scan (1.5 s, one at a time, the clock keeps running; a held paper can be dragged straight onto it); the paper returns to where it was picked up and its scanned copy reaches the PC (the Investigation app's Documents tab); a paper dropped while the scanner is busy slides back (scan states, drop outcomes and one-at-a-time tested: `DeskPapersTests`)
- [ ] Day 1 desk notes: by the scanner ("Click a paper to read it; drag it onto the scanner to open it on the PC.") until the day's first read (a paper lifted into the hand) or scan, and above the traveller ("Click the traveller to talk and ask for papers.", two lines, twice its old size) until the wheel is first opened that day; both notes are light text on a dark plate fitted to the text (`NoteBacking`), readable over the pale morning crowds and the evening palette (when each shows tested: `DeskPapersTests`)
- [ ] Traveller wheel: click the traveller or the desk intercom (the art's phone) while they are at the desk; the interview's choices on an elliptical ring (up to 8), grouped by kind (the way back, requests, questions, looks, dialogs, then a dialog's replies; a sub-menu entry counts as what it opens), each with its kind's icon at its left (white placeholder glyphs drawn at runtime, never letters, until art named `wheel_<kind>` is put in `Resources/WheelIcons`), sub-menus in place with "< Back" in the centre; Escape or a click outside closes it; papers held in the hand dip below the ring while it is open; a document request or a look at a garment closes it (layout and fit tested: `RadialLayoutTests`; kinds, order and icon names tested: `DialogChoiceKindsTests`, `InterviewScriptTests`; placeholder glyphs tested: `WheelIconPlaceholderTests`; the icons on the ring were checked in the art office: office-move spec)
- [ ] Speech bubble: above the traveller (clear of the wheel), they say their claim when they step up (in the office view), then their reply to each wheel choice; lines play one after another and type out (`DeskConfigSO`: 40 characters a second, 0 = at once) in reading order: a line shown in a right-to-left script types from its right end, digits and English inside it in their own order, every character keeping its place (tested: `DisplayTextTests`, `ArabicShaperTests`); the last line stays 4 s once complete, a line followed by another at least 1.5 s; a new reply waits for the line being shown and never cuts it; a choice without a reply ("Ask about home >", "< Back") adds nothing; opening the PC, or the traveller leaving, clears it; an answer line is compare-clickable (hand cursor; the bubble lights up while the picked answer shows; the same pick as its transcript row); hovering the bubble holds its line (its hold runs from when the pointer leaves; queued lines wait); the bubble sits above the open wheel (which lines are said tested: `InterviewScriptTests`; pacing, the tag and the hold tested: `SpeechQueueTests`; the bubble's drawing was checked in the art office: office-move spec); from translation's first day the lines are in the claimed place's tongue with their key words in English (see Translation), and with the region's Speech translator a line types in its script while its letters flip into English one by one behind the typing (the key words never flip), its hold starting once the flip has finished; opening the wheel finishes the flip at once; reduced motion shows the English as it types (timing tested: `FlipSequenceTests`, `DisplayTextTests`; the hold tested: `SpeechQueueTests`)
- [ ] The art's desk objects react to clicks (squash, wobble, nudge, pulse on the art itself; `DeskReactionSO`): stamp, phone, till, stability monitor, calendar, clock, calculator, pen pot, stapler (poses tested: `ReactionCurveTests`); named desk slots stay for decoration later; the stamp, while a traveller is at the desk, opens the stamp tray above it (Accept left, Deny right, the PC buttons' labels and glyphs; modal like the wheel: Escape or a click outside closes it); a choice decides the case like the PC's buttons (the PC, the props, the papers and the traveller are inert while it is open; tested: `BoothRulesTests`)
- [ ] The office case HUD, top centre, while a traveller is at the desk and the PC frame is closed: the claim tag (the PC banner's text: name, role and claim, always readable) and the office compare strip (the PC compare bar's text and colours); the verdict line and the citation slip show over the office (the slip still pauses the day until Acknowledge) (when it shows tested: `BoothRulesTests`)
- [ ] Desk view: a click on the mat (the desk's own area, where no paper or prop is), with no paper held and the wheel and the stamp tray closed, tilts the camera forward over the desk (0.35 s, eased; a cut with reduced motion) so the papers lying on it are easier to see; the same click tilts it back, and so do Escape (one thing per press: the frame, else the wheel or the stamp tray, else held papers go back, else the desk view returns) and a right-click on empty space; while tilted a small "▲ Back" control shows at the top centre of the office, under the case HUD (its hover hint "Esc / right-click"), and a click on it or the mouse wheel rolled up tilts back, papers held or not (not while the PC frame, the wheel or the stamp tray is open); the mouse wheel rolled down with the pointer on the empty mat (no UI, paper or prop over it) tilts in, under the same conditions as the mat's click; with papers held a click on the desk only puts them back; calling the next traveller (NEXT) and the shift report return to the normal view, while opening the PC keeps it; the pose comes from the art office's camera and the mat (`DeskConfigSO.deskView`: 0.6 m forward, 0.3 m up, aimed 4° above the mat's centre) as a gameplay Cinemachine camera the art camera's brain blends to (the art camera never moves); papers, props, drags, the scanner, the stamp tray, the wheel and held papers work in both views, and the speech bubble and the wheel's ring wait at the screen's top, below the case HUD's strips and the Back control, while the traveller's head is above it (when each input is live, the Back control and the wheel included, tested: `BoothRulesTests`; the pose tested: `DeskViewPoseTests`)

## Fake-OS desktop

- [ ] XP-style wallpaper, taskbar with Start button, a "< Desk" button, one button per open window (its title; at most 200 units wide, shrinking to fit as windows open; the focused window's pressed, a minimised one's faded; a click minimises the focused window, else restores and focuses it) and system tray (Day / Credits / Stability / Clock): the neutral theme; while a country leads the Future its theme replaces the colours, fonts, wallpaper and flavour labels (see The PC reacts to history) (the taskbar rule tested: `WindowStackTests`)
- [ ] Start menu: the six apps in the desktop's order, each opening its app through the desktop's app registry (`DesktopApps.OpenApp(id)` with the PC redesign's ids `investigation`, `internet`, `mail`, `citizen_account`, `notes`, `settings`, the same call as its desktop icon): Investigation, Internet, Mail (with the unread count, "Mail (2)"), Citizen Account, Notes, then Settings (its window shows with or without a traveller at the desk; a window in titled sections: Language: Follow history / Always English, remembered per player outside the run save, applied at the next office load; colours, fonts and wallpaper follow history either way; Motion: Full / Reduced, remembered per player outside the run save, applied from the next traveller: reduced shows translated speech at once, with no flipping (speech is the only text that flips; tested: `DisplayTextTests`); Desktop: open icons with Double click (the default) / Single click, remembered per player outside the run save, and Reset icon positions (the default arrangement, saved); Keyboard: Show shortcuts opens a card of the desktop's keys today: Escape's order, the icons' clicks, drag, arrows and Enter, the desktop's right-click, and the title bar's double-click), then Arrange icons, Turn off screen, Quit game; nine fixed-height entries; opening an app closes the menu; the menu opens above the compare dock; a press anywhere outside it closes it, and so does Escape (before the frame closes)
- [ ] Six desktop icons, nothing else, ever: Investigation, Internet, Mail, Citizen Account, Notes, Settings (no scan, book or case adds one; the ids are one contract, `DesktopAppIds`); an icon is a glyph (a code-drawn placeholder until the art lands; tested: `DesktopIconPlaceholderTests`) over its label (a flavour label, at most two lines) on translucent plates, with a badge at the glyph's top right (a count, "99+" above 99, or a dot; Mail's is its unread count; tested: `DesktopLayoutTests`); the icons sit on the wallpaper under every window, show between travellers too, and are never dimmed or gated (the old upgrade gate is gone); a press selects an icon (its plate in the theme's selection colour) and a press on the empty desktop deselects it; a double click opens it (the two clicks at most 0.4 s and 6 units apart: `DesktopConfigSO`; tested: `ClickTimingTests`), or a single click with Settings' choice; with the frame open and no window or text field focused, the arrow keys move the selection to the nearest icon that way and Enter opens it; an icon drags anywhere past the drag threshold, keeping where it was grabbed (kept inside the icon area, the desktop above the dock and the taskbar), and on release it stays exactly where it was dropped unless it covers more than 25 % of another icon's cell, when it moves to the nearest free arrange spot; Escape during the drag puts it back; positions are saved per player outside the run save (PlayerPrefs `TimeDesk.DesktopIcons`, so New Run keeps them) and restored at every office load (unknown ids dropped, a new icon in the first free spot, an off-screen place clamped); Arrange icons (a right-click on the empty desktop, the Start menu, Settings' reset) lays them out column-first from the top left in the default order and saves; a right-click on an icon offers Open; the context menu closes on a press outside it or Escape (first in the Escape chain) (layout, drop, restore, save and arrow rules tested: `DesktopLayoutTests`; the Escape order tested: `DesktopEscapeRuleTests`)
- [ ] Investigation app (the Investigation icon, the Start menu, Mail's directive memo; built by Build Office UI; one window of the window stack that opens maximised the first time and so fills the PC screen, restored 1120 × 820 units, `DesktopConfigSO`): every case source in one window. Its title reads "Investigation · <traveller>" while one is at the desk (its taskbar button follows the title); its case header holds the claim (the claim banner, as on the office claim tag), the counters "Papers 1 of 3 received · 1 scanned · Deviations 0" and the PC's Accept and Deny (off between travellers); its toolbar holds Back, Forward, the search field ("Search this case and today's records"), Steps, Split and F1 Keys, shown but not live yet; its sidebar holds Steps, Pinned and Recent, empty yet. One pane holds six tabs, in their default order Documents, Records, Reference, Transcript, Report and Rules, over a chip row for the active tab's items (a paper, a book); between travellers the case's tabs (Documents, Transcript, Report) show "Waiting for the next traveller" (80 units) and the day's tabs (Records, Reference, Rules) still work (the order and the case's tabs tested: `TabOrderTests`). Each tab's view sits behind one interface (`IAppView`), so the forms engine's views replace today's one tab at a time
- [ ] Nothing steals the view: a new traveller and the decision open or close no window (the app, open or not, stays as it is; a new case shows its Documents tab, clears its badges and turns the Reference tab's "Claimed place only" on); a new transcript line and a logged deviation badge their tab unless the player sees it (the app showing on that tab) and dot the desktop's Investigation icon while the app is closed or minimised; showing a tab clears its badge and showing the app the icon's dot (tested: `AppBadgesTests`). A finished scan is the one thing that opens by itself: it opens the app only when the app is closed (a minimised app stays down), shows the paper only when the Documents tab shows none (never replacing the paper the player reads, never switching the tab), badges Documents unless the player sees it, and always shows a toast above the windows, "<paper> scanned" with Open, for 4 s (`DesktopConfigSO`) (tested: `ScanArrivalTests`)
- [ ] Windows (app, document, reference) through one window stack, on a window layer that is the icon area (the desktop above the compare dock and the taskbar) and is not part of the case overlay, so an app opened between travellers shows, with its taskbar button: a press anywhere in a window focuses it and brings it to the front; min/max/close chrome on a 36-unit title bar (titles at 20); minimise keeps the window's taskbar button; maximise fills the icon area exactly (no insets), a double-click on the title bar toggles it, and a maximised window does not drag; a window remembers its restored place for the office session; a press on the empty desktop or an icon leaves no window focused; windows draw over the icons, and never over the dock or the taskbar; windows are dragged by the title bar and stay inside the icon area while dragged (a window taller than it keeps its title bar visible), and Escape during a drag puts the window back (Escape first closes the context menu, then leaves a focused text field, then closes the Start menu, then cancels a window or icon drag; only then does it close the frame) (clamp tested: `DeskGeometryTests`; the stack tested: `WindowStackTests`; the double-click tested: `ClickTimingTests`; the Escape order tested: `DesktopEscapeRuleTests`)
- [ ] Mail: an inbox (the messages newest day first; within a day the citation notices, latest traveller first, then the directive memo, the Times issue and the authored mail; unread ones in bold with a bullet, each row's day and sender under its subject; the open one marked) and the open message drawn as an internal memorandum, form TC-950 (TO, FROM, DATE in the agency's calendar and REF boxes, the subject, the body, a link, the signature and a RECEIVED stamp): each day's directive memo (that day's travel rules; its link opens the Investigation app on its Rules tab), each day's "The Temporal Times, day N" (the day's paper from the News site's archive, today's from the morning paper, with where the edition is: today's on the News site, an earlier one in its archive; an issue the archive no longer keeps is only its link; its link opens that issue on the News site in the Internet app), a citation notice with the slip's text for each citation once its slip is acknowledged (the shift's ledger holds them, so they are read that day), and the authored messages of `world_source.json` `pc.mail` (a message arrives on its first day, stays until its last day, 0 for the run, and waits for its story flag, if any); every message is rebuilt from its sources by id and only the read flags are saved (`WorldState.mailRead`); opening a message marks it read; the unread count shows on the Start menu's Mail entry, as the Mail icon's badge and in the window's title, "Mail (2 unread)", which its taskbar button shows (inbox, order, sources, badge and the authored mail's checks tested: `MailboxTests`)
- [ ] Citizen Account: the clerk's own account, not a traveller's (a traveller's record is in Citizen Records): the Record Extract, form TC-901 (the holder's line; RECORDS: name, birth date, lineage, Citizen ID 773-2840-19 from `agency.clerk`, status Eligible, standing Good, debt, balance (the wallet); FORMS ON FILE: the employment; TRAVEL: the booked departure, none; the note) with no pickable row, and below it the Statement, form TC-960: a row per day (DAY, WAGES, FINES, DEBT RELIEF, HOUSEHOLD, PURCHASES, BALANCE, OWED; the unit, "Departures are voluntary." and an empty stamp box) written at the shift's end (the ledger's pay and citation penalties, the wallet) and at Home (household costs, purchases, the wallet after) into `WorldState.accountDays` (at most `GameConfigSO.accountDaysKept`, 60, the oldest dropped); the debt and the Debt Relief instalment read "–" until the clerk's debt exists (the Citizen Account reads them through its source, `IClerkAccountSource`) (rows, the statement's rules, the cap and OWED following the instalments tested: `AccountTests`)
- [ ] Notes: a list of days (today first) and the chosen day's page: CLIPPINGS, pasted clips as cards (the text, where it came from, X to remove) grouped under each traveller's name, clips from no case last, at most 40 a page ("Paste clipping" pastes the system clipboard's text); NOTES, one multi-line field of at most 2,000 characters with its counter ("25 / 2000"); an empty page shows a hint; each day keeps its own page, saved with the run (`WorldState.notes`, at most 30 days, the oldest dropped; the limits are `DesktopConfigSO` knobs; a quit mid-shift loses that shift's notes, as it loses the shift) (pages, clippings, grouping and the limits tested: `NotesTests`)
- [ ] Internet (the Internet icon; built by Build Office UI; a window of the window stack that opens maximised the first time): Back, Forward and Home (the start page: a tile per listed site with its name, blurb and address), an address field (Enter or Go opens a typed site name, id, domain or `chronet://` address; anything else shows the missing page with the way home) and, on the Lineage Archive, its name search (keeping the page's country and era filters); links in pages navigate and every visit goes on a back/forward stack (`DesktopConfigSO` "Internet", 30 pages; a new visit clears Forward; a link to the page shown adds none); each opening gathers the run anew, so the pages show the day as it stands. Sites are data (`world_source.json` `pc.sites`: News, History, Ancestry, or Static with authored pages), listed from their first day, none gated. **The Temporal Times**: today's front page is the morning paper (the first news line is the lead, the other news goes under World, the notices are boxed, the timeline standings come from last night's ranking) with links to every back issue; a back issue shows its own day's paper; the archive lists them newest first. **Chronopedia**: an index of every country in every era (a Future place only while its nation leads), an article per place (its name, country, era and year, its moment, and an infobox of capital, ruler, currency, language, device and dress as history stands today; a revised value says "Revised on day N (was X)"), the present (the leader's Future place, else unsettled) and Revisions (every latched edit and pending carry, newest first: day, place, fact, before, after, why); an article links its place's people in the Lineage Archive. **Lineage Archive**: cards for the premades who are who they claim (never an impostor) and the authored people (name, born, died, place linking to its article, occupation or note, relations linking to other cards), a name search with country and era chips; generated travellers get no card. Page content is drawn in each site's fixed style (the culture theme never touches it; every text colour passes 4.5:1 on its page and boxes); no value on a page is compare-pickable (pages, links, words and styles tested: `SitesTests` incl. a crawl of every page reachable from the start page, `NavHistoryTests`, `NewsArchiveTests`, `NewsPagesTests`, `HistoryPagesTests`, `AncestryPagesTests`, `SiteStylesTests`; the browser checked in Unity)
- [ ] The Investigation app's Transcript tab: the current traveller's interview (speaker + line, 10 per page, long lines wrap onto two, jumps to the newest page); answer rows are compare-clickable (the bubble's answer lines are too); a wheel choice that adds lines (a question, small talk, a spoken request, a dialog reply) badges the tab and opens nothing; each new case starts it anew; the traveller's lines are in the claimed place's tongue, their key words in English, exactly as the bubble shows them (see Translation), and show wholly in English only with the Speech translator (settled, never animated); an untranslated answer is still compare-clickable and shows in the bar as "(untranslated <tongue>; <pack> Translator)"
- [ ] The Investigation app's Rules tab: the day's travel directives (Mail's directive memo opens it)
- [ ] The Investigation app's Records tab (Citizen Records): under the agency's printed name and programme line, type a name or an agency number → the agency's record, found by a whole number first, then an exact name, then the first name containing the query (tested: `CitizenRegistryTests`); the status line reads the query, "1 record on file" or "NO RECORD ON FILE", and today's date in the agency's calendar; a record is rows (a label, a value and, for evidence, a category) in titled groups, listed a page at a time (a group's title as a heading line); a 2150 citizen's record is their Citizen Account, in the art's three groups (traveller types §4.1): RECORDS (Name, Citizen ID, Born, Status "Premium", Standing "Good", Debt "0 cr", Lineage), FORMS ON FILE (Transponder "Hopper Mk II · HP-40718", Transponder Class, and "None on file" for a Stranding Waiver, a proof of means and a Labour Contract), TRAVEL (Booked departure: the destination; Departure date: today; each past trip, "4 Mar 2150, Periclean Athens (Ancient), returned", or "None on file") and the Note, found by the Citizen ID too; its Name, Citizen ID, Born, Status, Debt, Transponder, Transponder Class and Booked departure rows are compare-clickable, the rest shown only (rows and lookup tested: `AccountMakerTests`); a displaced traveller's record is their Displacement Registry entry: one REGISTRY ENTRY group of Name, Displacement No., Born, Origin, Incident, Found, Status ("Awaiting return") and the clerk's Note (a premade's own note), found by the Displacement No. too (records tested: `CitizenRegistryTests`; the entry's rows are checked in Unity); the Name, Displacement No., Born, Origin and Incident rows are compare-clickable, keyed by their record, so the same row of two records is two picks (tested: `ComparePairTests`), and only the traveller's own record proves their lie; the clerk's own account is in the registry too, as the Citizen Account app shows it that morning (`Account.ExtractRows`, in its groups), found by 773-2840-19 or the clerk's name, and none of its rows is compare-clickable (the clerk is nobody's case; tested: `AccountMakerTests`), and no traveller is ever given the clerk's Citizen ID; a liar's record is their cover identity (claimed origin), so records never reveal a true home; the rich desk warns once at start when the evidence system is active but Citizen Records is not wired (birth-date tells need it); likewise the rich desk warns once at start when the traveller wheel, the interview transcript or its window chrome is not wired: that day questions are hidden, no answer is computed and no tell is spoken, and the wheel offers only the requests (and the look); likewise when the wheel or the compare bar is not wired: garments cannot be looked at and no dress tell is generated that day; likewise it warns once when the desk scanner is not wired: documents then open on the PC when handed over

## World

- [ ] Real countries as lineages: Egypt, Iraq, Greece, Italy, China, Japan, Britain, Germany × Ancient / Medieval / Early modern / Industrial / Modern = 40 places, plus eight Future places (the office's own time, 2150), of which only the leading nation's is in the world (see History)
- [ ] The present, 2150 (traveller types H1): where 2150 citizens come from and never a destination; the leading nation's Future place while a nation leads, otherwise the neutral present authored in `world_source.json` `present` (Temporal Customs Zone: Credits, Agency Standard English, Wrist comm, Directorate Tower, The Customs Directorate, and a Culture fact derived from its wardrobe, "tech jacket / coat-dress"; the choice tested: `PresentTests`); `TodaysWorld.Present`, and its row is added to today's facts after today's places, once, so every reference book lists it from day 1 (tested: `PresentTests`, `FactTableTests`)
- [ ] The present, 2150 (traveller types H1): where 2150 citizens come from and never a destination; the leading nation's Future place while a nation leads, otherwise the neutral present authored in `world_source.json` `present` (Temporal Customs Zone: Credits, Agency Standard English, Wrist comm, Directorate Tower, The Customs Directorate, and a Culture fact derived from its wardrobe, "tech jacket / coat-dress"; the choice tested: `PresentTests`); `TodaysWorld.Present`, and its row is added to today's facts after today's places, once, so every reference book lists it from day 1 (tested: `PresentTests`, `FactTableTests`)
- [ ] The present, 2150 (traveller types H1): where 2150 citizens come from and never a destination; the leading nation's Future place while a nation leads, otherwise the neutral present authored in `world_source.json` `present` (Temporal Customs Zone: Credits, Agency Standard English, Wrist comm, Directorate Tower, The Customs Directorate, and a Culture fact derived from its wardrobe, "tech jacket / coat-dress"; the choice tested: `PresentTests`); `TodaysWorld.Present`, and its row is added to today's facts after today's places, once, so every reference book lists it from day 1 (tested: `PresentTests`, `FactTableTests`)
- [ ] Each place has a moment and year, five authored facts (currency, language, technology, capital, ruler), a Culture (dress) fact derived from its wardrobe's signature items ("top hat / poke bonnet", men's / women's, or one item for both), a wardrobe per gender (outfit, hair, facial hair, headwear, accessory; one signature item each) and 8 male + 8 female period names (the Culture value and the label rule tested: `LooksTests`)
- [ ] Travellers are born 18–70 years before their place's year (set by the generator); a 2150 citizen before the present's year (2080-2132); ancient dates print as BCE (dates tested: `BirthDatesTests`)
- [ ] Visitor roles: Artist, Diplomat, Merchant, Scientist, Soldier, Wanderer (their accepted sends move Democracy / Science / Art); roles never change a traveller's dress; names come only from the claimed place's period names (a liar's name is part of their cover); a 2150 citizen's name comes from the eight Future places' lists together (64 men's and 64 women's names, none on both merged lists; checked by Generate World and the validator), with the same one case-stream draw (tested: `CitizenNamesTests`); a 2150 citizen's name comes from the eight Future places' lists together (64 men's and 64 women's names, none on both merged lists; checked by Generate World and the validator), with the same one case-stream draw (tested: `CitizenNamesTests`); a 2150 citizen's name comes from the eight Future places' lists together (64 men's and 64 women's names, none on both merged lists; checked by Generate World and the validator), with the same one case-stream draw (tested: `CitizenNamesTests`)
- [ ] Every traveller has a kind (`TravellerKind`: rich tourist, poor tourist, labourer, displaced; one blueprint per kind, `CaseBlueprintSO.kind`, the blueprint pick costing no new draw); the day's kinds weigh the pick (see Run & meta). A rich tourist (`CaseBlueprint_RichTourist`) is a 2150 citizen travelling for fun: they claim a destination, one of today's past places, and come from the present; their name and birth date are 2150's, they speak English, they wear the destination's dress, and their papers print their Citizen Account; in this phase they are always honest (a citizen draws nothing on the lie stream: `TravellerKinds.MayLieAboutPlace`, tested: `TravellerKindsTests`; their record lies come in phase 7). A displaced traveller (`CaseBlueprint_Displaced`) asks to go home to the place they claim, and their name, birth date, papers and Citizen Record all come from that claim. An honest traveller really comes from there: every field agrees with the books and the record. A liar really comes from another of today's places (any country, any of today's eras) and travels under the claimed place's cover identity, but the disguise leaks tells carrying the true home's value, on the papers (a Currency, Language or Technology tell rewrites every field of that category; a birth-date tell keeps the record's day and month with a year from the true home's birth years), in speech (their answer to one of today's day-gated questions gives the true home's value while the papers show the cover) or in dress (one garment, the true home's signature item for their gender, worn over the claimed look). A category leaks on one channel only. The day plan's tell channels set where tells may appear (day 1 papers only; day 2 papers and answers; from day 3 papers, answers and dress); a spoken tell needs its question askable that day, gated by day alone, and a book (or the Citizen Record for a birth date). Each liar leaks the day's tell count (`DayPlanSO` "tell count", 1 on days 1–6), capped by the categories that can carry a tell for them. The liar chance is the blueprint's contradiction chance plus the slot-machine modifier and forgery-risk effects; rule violators and honest premades never lie (a premade authored as a liar always lies, from their own true place, through papers or answers, never dress); a rolled liar with no possible tell stays honest and a warning is logged (tell selection, channels, eligibility and the may-lie exemptions tested: `LiesTests`, `ForgeryTests`, `InterviewDayTests`, `LooksTests`; answers tested: `InterviewTests`; the liar chance, cover records and whole-day behaviour are checked in Unity, not by the EditMode suite)
- [ ] Traveller gender is recorded from the claimed place's name lists (a 2150 citizen's from the merged Future lists, tested: `CitizenNamesTests`; "Marcus II" counts as Marcus; premades carry an authored gender; "Subject #n" is unknown, and their look draws a gender); the desk's opener uses it for the honorific (sir/madam/traveller) (tested: `TravellerGendersTests`, `NameRosterTests`, `InterviewTests`)
- [ ] Premade characters: ten named, long-dead, non-ruler people who lived in their claimed place and era (five women, five men), drawn whole; none claims or comes from the Future (its place is in the world only while its nation leads); Senenmut stands in day 1 slot 3 and "Socrates" (an impostor from Republican Rome) in day 2 slot 6; every other slot of days 2–6 has a 5% chance of one from the day's pool; once per run when presented (a met forced premade leaves an ordinary traveller in the slot); never on a rule violator's slot, and violators are never placed in a forced premade's slot; honest unless authored as liars; their own opener and Citizen Records note; scoring as legendaries (bonus pay, extra stability risk) (scheduling tested: `PremadesTests`, `ViolatorSlotsTests`; the opener tested: `InterviewTests`; the met flag's format tested: `GatesTests`; whole-day scheduling checked in Unity)

## Investigation loop

- [ ] Claim banner (visitor name + the claim in their kind's words, `world_source.json` `interview.claims`: the displaced say "Please. Send me home to <place> (<era>).", rich tourists "One leisure departure to <place> (<era>), please."; a kind with no line shows the place alone), in the Investigation app's case header and on the office claim tag (the claim per kind tested: `InterviewTests`, `InterviewScriptTests`)
- [ ] Traveller wheel = the interview: every entry is a dialog choice. Hub: "Request <document>" for a traveller's one document handed over on request, or, for two or more, one entry, "Request papers >" (the papers menu: "< Back" in the centre, then one entry per such document, named by its form; the displaced: the Intake Declaration and the Return Order) (each request one-shot; the traveller hands it over onto the desk, or straight to the PC where no desk is wired; a document handed over on arrival, the displaced's Displacement Certificate, lands on the desk, or reaches the PC, when the traveller is presented; `DocumentTemplateSO` hand-over), the spoken requests authored in `world_source.json` `interview.requests`, today "Step closer" and "Speak up" (the desk asks and the traveller answers, in the bubble and the transcript, never evidence; one-shot; nothing else happens), "Ask about home >" ("< Back" first, in the wheel's centre, then today's questions, one-shot per traveller, and small talk) "Look >" (the traveller's visible garments, each labelled with its item's name, "< Back" in the centre; choosing one puts it into the compare bar as `Traveller · <slot>` with the item's name, to compare with a Costume Guide row on the PC, and closes the wheel; a premade shows one garment, "Period dress") and today's narrative dialogs (a premade's own dialog only while they are at the desk); content never offers more choices than the wheel shows (8; the hub's worst case is exactly 8 today: the papers menu, 2 spoken requests, ask, look, 2 dialogs and a premade's dialog; the papers menu counts as one hub entry and is checked on its own), and the way back and the requests come first (hub, menus, the papers menu, the look menu, spoken requests, one-shot requests and which documents get one tested: `InterviewScriptTests`, `DialogRunnerTests`; premade-bound dialogs tested: `InterviewDayTests`; locked questions hidden and dialogs offered tested: `InterviewDayTests`; capacity rules tested: `InterviewScriptTests`)
- [ ] Questions: Currency, Language, Device from day 1; Capital from day 2; Ruler from day 3 (each announced in that morning's paper); Date of birth once Interview Protocols is owned (announced the next morning). Unlocks are fixed at the start of the day. Only day-gated questions can carry a spoken tell: the birth-date question is always answered with the registered date, a hint against a certificate's birth-date tell (gates tested: `GatesTests`; availability and tell eligibility tested: `InterviewDayTests`)
- [ ] Honest answers equal the claimed place's book entries and the Citizen Record; a liar answers with the cover except for a spoken tell; small talk comes from the claimed place (or its era) and is never evidence (the answer rule, the cover unless the category is an Answer tell, and the small-talk order, place lines before era lines, tested: `InterviewTests`; small talk never evidence tested: `InterviewScriptTests`; which cover values and small-talk lists `CaseFactory` passes in, the claim's and never the true home's, is Assembly-CSharp and checked in Unity (spec §6 steps 3.5 and 3.10), not by the EditMode suite)
- [ ] The Investigation app's Documents tab: a chip per paper of the case, in paper order, reading where it is ("<name>: on the desk", "<name>: not handed over"; a scanned one reads its name), and the chosen paper's scanned copy (a paper not scanned shows why instead: "On the desk: scan it to read it here." or "Not handed over: ask the traveller for it."; the papers' states and the header's counters tested: `CasePapersTests`), a SCANNED page (white page on dark scanner backing; the primary form's first page carries the traveller's photo, its rows leaving room for it), multi-page, structured fields
- [ ] Every paper is a Temporal Customs agency form, filled in English: a `DocumentTemplateSO` with its form number ("TC-610") and the kinds the desk may ask for it (`askableBy`). The displaced carry three: TC-610 Displacement Certificate (on arrival, with the photo: Full Name, Displacement No., Date of Birth, Origin, Incident, Valid Until), TC-620 Intake Declaration (on request: Declarant, Displacement No., Coin of Home, Native Tongue, Effects Carried) and TC-630 Return Order (on request: Returnee, Displacement No., Return To, Incident, Departure). A rich tourist carries two: TC-101 Leisure Departure Visa (on arrival, with the photo: Full Name, Citizen ID, Date of Birth, Destination, Visa Class, Valid Until) and TC-230 Departure Manifest (on request, askable by every 2150 citizen, a direct "Request Departure Manifest" at the hub: Citizen ID, Transponder, Transponder Class, Currency Carried, Declared Effects, Departure). A traveller's forms print one value per category, the one their record holds (name, Displacement No. or Citizen ID, birth date, origin or destination, incident; a citizen's status, transponder and its class), the claimed place's facts (the manifest's currency and effects are the destination's), today's date as the departure and each expiring form's honest Valid Until; no date is checked yet; the identity rows are checked against Citizen Records (the forms' values are checked in Unity)
- [ ] The Investigation app's Reference tab: a chip per book, and the chosen book's register of TODAY's places only, then the present's row (from day 1) (country then era, paged; the claimed place's row first, reading "(claimed)"; "Claimed place only", on at each new case, shows that row alone; the Costume Guide groups the others under era headings, the claimed era first, the present's era last, `ReferenceBookSO.groupByEra`; tested: `ReferenceRowsTests`), read from the day's `FactTable` snapshot — the same values printed on papers (table tested: `FactTableTests`; the day's place filter is covered by the content validator and the Unity world check)
- [ ] Reference books: Currency Ledger, Tongues & Scripts, Index of Devices, Capitals Gazetteer, Rulers & Regents, Costume Guide (each place's signature dress, men's / women's, and the present's) (all in the Reference tab from the first traveller)
- [ ] Click-to-compare any two values from the desk and the PC: a held paper's boxes, the bubble's answer, a garment (Look), and every PC row; one comparison shown in the PC's compare dock and the office strip (the dock: a strip across the desktop right above the taskbar, over every window, while a traveller is at the desk; empty, it reads "Click two values to compare them"; its X clears the comparison); MATCH/MISMATCH (visual, no auto-verdict; the words in the culture's language with the English in brackets, colours per theme); auto-sized text; picking a picked value again clears it, on either surface (the same field on the paper and on its scanned copy is one value); a pick waits across the PC frame's opening and closing; a garment shows its item name and matches on its place's Culture value, so it MATCHes its own place's Costume Guide row (the compare rule tested: `DiscrepancyLogTests`; pairing, clearing and keys tested: `ComparePairTests`)
- [ ] Deviation Report (the Investigation app's Report tab): true contradictions auto-register from a document field, a traveller's answer or a garment they wear (tested: `DiscrepancyLogTests`)
  - [ ] Mismatch proof: a liar's tell (printed, spoken or worn) ≠ claimed-era reference entry
  - [ ] Match proof: a liar's tell (printed, spoken or worn) = a *different* era/nation's entry (origin proof); it names the traveller's true home
  - [ ] Record proof: a birth-date tell (printed or spoken) ≠ the traveller's own agency citizen record (its Born row); another person's record, or a record row with no value, proves nothing (tested: `DiscrepancyLogTests`)
  - [ ] Junk comparisons never register (wrong category, foreign-era mismatch, honest fields, answers and garments, answer vs papers, answer vs answer, garment vs papers or answers)
  - [ ] One discrepancy per category from any source; a second proof of a documented category shows "ALREADY DOCUMENTED" in the compare bar and adds nothing; cleared per case; a new deviation badges the Report tab and counts in the app's header (nothing opens)
  - [ ] The compare dock and the office strip flip to a "DEVIATION LOGGED — …" verdict in the theme's mismatch colour when evidence registers (red on the neutral desk, slate under China; never a MATCH)
  - [ ] Reports say where the tell was ("papers show …" / "traveller said …" / "traveller wears …") and label Geography CAPITAL, Politics RULER, Technology DEVICE, Culture DRESS and birth dates BIRTH DATE (the key choice tested: `DiscrepancyLogTests`; the English words and sentences live in the UI string table)
- [ ] Accept / Deny decision buttons, in the Investigation app's case header (off between travellers) and on the desk stamp's tray, each with a fixed tick or cross that never changes with the culture (Accept always left); the verdict line sits on a strip over the office while it shows
- [ ] Without the Investigation app the office builder wires (the app, its Documents tab's page, Accept and Deny) the investigation logs one error at start and shows no case; there is no text-mode fallback (deleted: audit R4-002)

## Translation

- [ ] Every document is filled in English: titles, labels and values, on the desk and on the PC, never in a tongue and never flipping (Saleh: "all documents must be filled in english"; the redesign's F5, which retired piece 9's papers translation and piece 10's shared written reveal and its sightings)
- [ ] Every place has a tongue (`world_source.json` `places[].tongue`); a traveller speaks the claimed place's tongue, never their true home's, so the tongue never shows or hides a lie; a 2150 citizen speaks English wherever they are going. English (Britain from the Early modern era, the Future included) is always readable. From day 2 (`translation.fromDay`, announced in that morning's paper by a one-shot trigger) a foreign tongue's traveller lines are drawn in its script, in the bubble and the transcript (the desk speaks English), but for their key words; digits, spaces and punctuation stay as they are, and the same line always looks the same (the day's rules and who speaks the tongue tested: `TranslationTests`; the glyphs tested: `PseudoscriptTests`)
- [ ] Key words: an untranslated traveller line keeps some words in English, enough to understand some of it (Saleh: "key words will still be in english enough for the player to understand some stuff in the dialogue. this we will tune later"), by a data rule (`world_source.json` `translation.keyWords`): the fill of each listed slot (the claimed place, a name, a document: "I request passage home to Periclean Athens (Ancient)." keeps "home" and the place), each listed word as a whole word, ignoring case and accents (home, please, papers, yes, no, Temporal Customs), and digits; an answer's value is not a key slot, so it stays in the tongue (a listed word or a digit inside it still shows); the key words are the same in the bubble and the transcript, are never drawn in glyphs and never flip, and a right-to-left line keeps them in their own order, a bracketed place label whole (the rule tested: `KeyWordsTests`; each traveller line's spans tested: `InterviewScriptTests`; drawing tested: `DisplayTextTests`, `PseudoscriptTests`, `FlipSequenceTests`, `ArabicShaperTests`); tuning edits the lists
- [ ] Scripts: hieroglyphs (Egyptian), cuneiform (Akkadian), Arabic (shaped, right to left), Greek, Han (Chinese), kana (Japanese), runes (Proto-Germanic) and foreign-looking Latin letters (Latin, Italian, Brittonic, Old English, German); each tongue maps a..z to its own 26 authored cells (Latin-1 letters folded first, capitals kept where the script has case); each script's fonts are OS fonts loaded at runtime and never copied (Segoe UI Historic for hieroglyphs, cuneiform and runes; piece 6's Arabic and CJK chains; the runtime LiberationSans for Greek and Latin letters); a text uses its script's font only while it shows foreign letters, and its own font whenever it does not, whatever it showed before (so a reused row gets its own font back), so the tracked TMP fonts never grow (the font rule tested: `DisplayTextTests`), and may shrink to fit meanwhile; a tongue whose script no installed font draws shows in a Latin-letter fallback cipher, with one warning naming the fonts tried
- [ ] Untranslated speech still compares on its original value: an untranslated answer (its transcript row or the bubble's line) stays compare-clickable, MATCH/MISMATCH and deviations are decided on the canonical value (the logged line quotes it in English), so every liar is provable without a translator; the compare bar never shows foreign glyphs: an untranslated side reads "(untranslated Egyptian; Near East Translator)" (the evidence code is unchanged; its rule tested: `DiscrepancyLogTests`)
- [ ] Translators: four regional packs (Near East: Egyptian, Akkadian, Arabic; Mediterranean: Greek, Latin, Italian; East Asia: Chinese, Japanese; Northern Europe: Brittonic, Old English, Proto-Germanic, German), each sold at Home as its Speech translator (80), in force from the next office day (fixed at the day's start), saved as owned upgrades; piece 9's Papers translators are no longer sold, and an old save that owns one keeps a harmless id (ids tested: `TranslationTests`)
- [ ] The flip is speech's only: with the region's Speech translator, a traveller's line in the bubble types in its script while its letters flip into English one by one behind the typing, each letter passing through two glyphs of its script (`translation.flip`) while the key words stay as they are; opening the wheel finishes it; the transcript shows the settled line; reduced motion shows the English at once (timing tested: `FlipSequenceTests`, `DisplayTextTests`)
- [ ] The scene behaviour of each line above is checked in the art office by the redesign plan's phase 1 play-through (day 2: a foreign traveller's papers in English held at the desk and on the PC, the untranslated bubble and transcript, the Speech translator's flip and reduced motion, an untranslated answer's proof with the placeholder, the Home shop's two pages; 1920 x 1080 and 1280 x 720); piece 9's check of the untranslated scripts, the placeholders and the proofs with no translator (translation spec §8) stands for speech

## Characters

- [ ] Generated travellers are layered figures (hair back, body, outfit, head, facial hair, hair, headwear, accessory) on one 1024×1536 canvas, dressed from the claimed place's wardrobe for their gender; skin tone and hair colour follow the claimed place's weights (a 2150 citizen's: their family country's, the Future place whose list gave their name) and are never tells; the face follows age (bands 18/35/60, grey from 60; age = the place's year minus the registered birth year, a citizen's counted from 2150); a worn item can hide another (a turban hides the hair); a liar with a dress tell wears one garment of their true home (its signature item) over the claimed look; an item may be filed under a shared art nation (`artNation`) instead of its place's nation, so places of one era can share one drawing (composition, draw order, coverage, keys and shared art tested: `LooksTests`, `LookKeysTests`)
- [ ] Premades are one whole picture with four expressions (neutral, happy, angry, worried); a premade's line with an expression changes their picture in the office as that line starts in the speech bubble (a line without one keeps the face; when the bubble is cleared early, the last expression not yet shown applies at once) (the expression rule tested: `SpeechQueueTests`; that lines carry their expression tested: `InterviewScriptTests`)
- [ ] Character art loads by file name (`LookKeys`) from `Assets/Art/Characters/Resources/Characters`, imported as a sprite one unit tall pivoted at the feet (`CharacterArtImporter`); a key without art is drawn at runtime as a colour-coded placeholder (the country's hue at the era's shade for garments, grey for a shared art nation that is no country, the hair colour, the art brief's skin swatches, a striped figure with an expression mark for premades) that is never written to disk; only the current traveller's art stays loaded (placeholder drawing and canvas geometry tested: `LayerPlaceholderTests`, `PixelShapesTests`, `PlaceholderPaletteTests`, `LookCanvasTests`; loading, the fallback and release are checked in Unity, not by the EditMode suite); the validator logs how many distinct keys have final art (a drawing shared through `artNation` counts once)

## Shift clock & queue

- [ ] Papers, Please-style clock: 09:00–17:00 over 8 real minutes (GameConfigSO "Shift clock"); starts at Start Shift (tested: `ShiftClockTests`)
- [ ] The day plan's visitor count is the queue size (8 / 10 / 12 / 12 / 13 / 14 on days 1–6; later days as day 6); the day ends at closing time or when the queue empties
- [ ] Closing: a traveller at the desk may be finished; one still behind READY is never called (tested: `DaySlotSequencerTests`, `ShiftFlowTests`, `ReadyGateTests`)
- [ ] Scheduled events, forced cases (blueprints) and forced premades that would have stood in slots never reached before closing are reported as a warning (a premade met on an earlier day is not named)
- [ ] The clock pauses only while a citation slip is shown, and the slip keeps the PC screen on until acknowledged (the interview, scans and camera moves take real time and cost nothing else)
- [ ] Visitor names are unique within a day ("Marcus II" once a pool runs out; a premade never repeats a name used that day; a forced premade's name is reserved for their slot before the day starts, and no premade's name is in any place's name list) (uniqueness tested: `NameRosterTests`; the reservation is checked in Unity)

## Interaction feedback

- [ ] Game cursor in every scene: arrow, or a hand over anything clickable (`InteractionFeedbackSO` via `RunConfig`; click points derived from the cursor art) (tested: `CursorHotspotTests`)
- [ ] Hover highlight: a white outline hull drawn around the hovered art in the office (the PC and its power knob, NEXT, the props, the scanner, papers); the traveller's hit zone gets the hand cursor only; an outline on desktop UI buttons and icons: the theme's two rings on themed UI (see The PC reacts to history), XP amber elsewhere

## Scoring & consequences

- [ ] Correct decision: pay (base × pay-rate multiplier + timeline bonuses; legendary bonus)
- [ ] Wrong decision: citation (free warnings, then escalating penalties), stability loss
- [ ] Evidence-gated denial ("Log a deviation from the papers, the traveller's answers or their dress before denying"): denying a liar with **zero** documented discrepancies counts as a wrong decision (citation and stability loss; a deduction once the free warnings are used) even though the visitor lied (`requireEvidenceToDeny` toggle) (the unproven-denial rule tested: `VerdictRulesTests`; the citation and deduction are Assembly-CSharp (`ShiftScoring`) and checked in Unity, not by the EditMode suite)
- [ ] Directive-violation denials never need scanned evidence (the gate exemption tested: `VerdictRulesTests`)
- [ ] Only provable tells are generated (tested: `ForgeryTests`, `LiesTests`, `BirthDatesTests`, `InterviewDayTests`, `LooksTests`): a place fact is a tell only when a reference book covers it and the true home's value differs from the claim's and belongs to no other of today's places (so the books prove it and the origin proof names the home); it shows on the papers only when a paper prints it, in speech only when its question is askable that day and gated by day alone, in dress only when the Costume Guide exists and the true home's signature item for the traveller's gender is a real, leakable item (never the whole outfit), not hidden by the disguise, not drawn from the same art as the disguise's own item in that slot, not labelled like it and not on the confusable list; a birth-date tell keeps day and month and takes a year from the true home's birth years, never the record's (provable via citizen records); names are never tells
- [ ] Every active travel rule gets at least one violator in the first half of the queue (`DayPlanSO` "guarantee rule violators", on by default) (slots tested: `ViolatorSlotsTests`)
- [ ] Timeline impacts apply only on ACCEPT and land on the claimed place (where the traveller is sent), liar or not; sends tracked per era; an accepted liar also carries their true home's Technology there (see History)
- [ ] A new run ranks every place's attributes (Future places have none) from the baselines silently; each night's news then reports a newly DOMINANT attribute only for places on tomorrow's plan ("Science is now DOMINANT in …"; no "rising" line) (tiers tested: `DominanceTiersTests`, `ScoreRankingTests`; the tomorrow filter is checked in Unity); the line's wording is content, `history.lines.dominant` in `world_source.json` (tokens {attribute} and {place}, required and checked by Generate World like the other history lines)
- [ ] Dominance tiers carry no gameplay effects: the Phase 7 tier effects were retired with the made-up world; history follows national influence instead (see History)
- [ ] Shift ledger (tested: `ShiftLedgerTests`); citation slip pauses the day (and the shift clock) until acknowledged
- [ ] Timeline triggers fire once per run at night: Art Renaissance (global Art total ≥ 20), Science Boom (global Science total ≥ 15), Democratic Collapse (Democracy ≤ −6, from the night of day 3) (condition types tested: `GatesTests`)
- [ ] Narrative dialogs may carry a consequence: an effect limited to instant ops (flags, counters, money, stability, upgrades, scores) and briefing/news lines; the generator and validator reject timed modifiers (pay, liar, legendary, shop, visitor and blueprint bonuses, cues) in a dialog's effect. A dialog with a consequence is one-shot per run. Consequences apply at the end of the shift, before the save: instant ops at once (the HUD refreshes and endings are checked), briefing and news lines in the next morning's paper. A dialog asset whose structure is broken (a node that cannot reach an ending, an effect on a non-ending choice, too many choices) is never offered and an error is logged (recording, one-shot memory and apply-once tested: `InterviewDayTests`; structure tested: `InterviewScriptTests`; the op rule tested: `EffectOpsTests`; the effect application is Assembly-CSharp and checked in Unity)

## History

- [ ] Each night every nation's influence is the sum of the attribute deltas stored for its places outside the Future (accepted travellers' impacts and `AddAttributeScore` effects; baselines, nation scores and global totals are not influence, so `AddNationScore` is no lever); the nations are ranked (saved in the history block). The top nation starts leading when its influence is above the floor (`GameConfigSO` "History", 4) and strictly ahead; a leader stays until one challenger alone beats it by more than the margin (2) or it falls to the keep floor (2); ties never make or change a leader; a new or lost leader makes the next morning paper (influence, ranking and the leader rule tested: `InfluenceTests`, `ScoreRankingTests`, `NationLeaderTests`; the nightly wiring is checked in Unity)
- [ ] The Future is the leader's: from the next day its Future place is in the world (a plan that includes the Future era, day 6 on, sends its travellers home there, and its facts fill the papers and books: a Chinese lead makes the Future pay in "Digital yuan (e-CNY)"; a liar may claim the Future or come from it); with no leader nobody travels from the Future and the books list no Future row; a Future traveller wears their Future place's culture-shaped outfit, its dress signature for both genders (Shanghai Megacity: "pankou cross-collar coat"), so a Future home never leaks dress, and a hairstyle per gender (and a men's beard) that all eight Future places share as one neutral drawing (`artNation` `neutral`: `hair_{g}_neutral_future_{colour}`) (which places are in the world tested: `HistoryTests`; the shared drawing and that an outfit never leaks tested: `LooksTests`, `LookKeysTests`; the era draw is checked in Unity)
- [ ] An accepted liar carries their true home's Technology (`GameConfigSO` carry category) into the claimed place: once a (home, claim) pair reaches the carry threshold (1), that place's Technology is the carried value from the next day, everywhere (books, papers, tells, answers), and the morning paper says so; honest travellers and violators never carry (carry recording and promotion tested: `CarriesTests`; that books, papers, tells and answers follow is checked in Unity)
- [ ] History rules (world_source.json `history.rules`, generated as one-shot timeline triggers whose effect holds `SetFact`; eleven ship: one per possible leader, e.g. a Chinese lead gives Florence a Chinese movable type press, plus three on attribute totals or tiers) rewrite a place's fact for good when their conditions pass at night (new conditions: a nation leads, a global attribute total at least / at most a threshold), with their news line; `SetFact` is allowed only in history-rule effects (conditions tested: `GatesTests`; the op rule tested: `EffectOpsTests`)
- [ ] The newest latched edit of a (place, category) wins; history edits Currency, Language, Technology, capital and ruler only; facts are built in one place (`ContentLibrarySO.FillFacts`) from night-latched history only, and place names and origin labels never change (resolution tested: `HistoryTests`; the one build is checked in Unity)
- [ ] History news: the leader line and carry lines are capped per night (`GameConfigSO` "History", 3), the leader's first, the rest logged (tested: `HistoryTests`); history rules' lines are authored one-shots; history news is English text
- [ ] While a nation leads, its leader effect broadcasts the UI cue `culture:{nation}` (the present culture; cue grammar and the choice between cues tested: `CultureCueTests`); the culture theme service receives it (see The PC reacts to history)
- [ ] Reference books mark a row whose value history changed with "[revised]" after its place, for as long as it differs from the authored value; the value and the compare evidence never change (rule tested: `HistoryTests`; marks tested: `FactTableTests`; the `FillFacts` hook not yet checked in Unity)

## The PC reacts to history

- [ ] Present culture: the timeline leader's culture (piece 5); before any country leads the PC is neutral (today's XP look). One persistent `CultureThemeService` applies the culture's theme to every `ThemeTag` in a scene when it loads (before its first Start, so the briefing never flashes the neutral look; `GameManager.Start` re-applies once the run exists), never during a shift; any scene's UI can be themed by tagging it (the office builder tags the gameplay layer's; the art office's own texts, such as the NEXT caption, keep the art's font and colour; checked in the art office for neutral and every culture: office-move spec)
- [ ] A theme sets each tagged graphic's colours by role, the wallpaper, the fonts (styles: italics stripped for CJK and Arabic, small caps or bold additions), the 28 flavour labels in the culture's language with an English gloss on the decision controls (Accept, Deny, START SHIFT, GO HOME, SEARCH, Acknowledge, "< Desk"; MATCH/MISMATCH in brackets), the compare colours (China's MISMATCH is slate) and the hover rings; evidence (document, book, record and transcript rows, the scanned page, the speech bubble; forms and Internet pages) keeps its look: the diegetic roles are an explicit list, so a role appended for new chrome is never diegetic; the retired role (the desk dim) takes no graphic and needs no colour (the diegetic and retired rules tested: `ThemeRolesTests`, `PaletteTests`)
- [ ] UI strings: every UI string of the office comes from the `world_source.json` tables through `UiText` (165 keys; the rest of the text is content and stays English), numbers formatted in the invariant culture, Arabic shaped and ordered for display and never broken inside a phrase (tested: `UiStringsTests`, `ArabicShaperTests`); the deviation lines, category words and garment-slot words are keys Domain chooses (tested: `DiscrepancyLogTests`, `LooksTests`)
- [ ] Fonts: installed OS fonts at runtime per culture, tried in order (a font file, then the family), ending in a runtime LiberationSans built from the project's TTF (Greece's font); never bundled or saved; a culture whose labels no font draws shows English labels in its colours, with a warning (the choice tested: `CultureChoiceTests`)
- [ ] Readable text: every text the game draws reaches WCAG AA on what it is drawn on (4.5:1, or 3:1 from 24 px, or 18.66 px bold, as drawn on a 1920×1080 screen; the PC's desktop is drawn at the frame glass's size), in the neutral theme and every culture's, over the office, on the desktop, at Home and on the Title (checked by a play-through audit of every state; an inactive control, a locked icon and a paper lying on the desk, read in the hand, are exempt); the neutral Start button and close X are darker (#2E7D32, #C43C2C) so white reads on them; the Start button, the claim strip and the screen strip need body text's 4.5:1 in every theme (they draw texts under 24 px); the held paper's row labels are darker, still quieter than their values; Build Office UI and Build Home UI log any text below its minimum in any theme (the size class and the worst case through a translucent stack tested: `ContrastTests`)
- [ ] Hover rings: themed UI is outlined by two rings from its theme, dark inside and light outside, at least 9:1 apart, so one reaches 3:1 on any background (ring rule tested: `ContrastTests`); untagged UI (Title, Home) keeps the settings' amber
- [ ] Wallet: named after the present culture's Future currency (the present's Currency, `ContentLibrarySO.BuildPresent`, which builds only that place, audit R4-018) in the tray, the verdict line, the citation penalty, the ledger, the till tooltip and at Home (money, expenses, shop prices, slot machine); Credits when neutral; long names shrink instead of wrapping (the choice tested: `CultureChoiceTests`)
- [ ] The present culture and the run's ending are separate: endings come from attribute totals, so an ending can differ from the desk's look

## Content & tooling

- [ ] `Tools > TimeDesk > Generate World` — builds the real world from the hand-maintained `Assets/Data/World/world_source.json` (eras, countries, 40 places with their wardrobes, look weights and the Culture facts derived from the wardrobes, the look rules (face bands, grey age, the premade garment label, confusable pairs), premades, rules, day plans with each day's tell count and tell channels, premade pool, forced slots (premades and blueprints) and chance, small talk, the interview's wording (with the papers menu's label and the claim per traveller kind, `interview.claims`: a line for every kind a blueprint makes, none twice, each with `{place}`, the rule the validator shares, `Interview.ClaimProblems`; tested: `InterviewTests`), spoken requests and menu capacity (its longest-line limit, `interview.maxLineChars`, stays in the source and bounds the line check below), questions with an unlock-announcement trigger for every gated question, and dialogs; the Future era and its places, each with its wardrobe (its culture-shaped outfit as the dress signature for both genders, the hair and men's beard filed under the shared art nation `neutral`); one leader effect per country; the history lines and history rules, each a one-shot trigger with a `SetFact` effect; conditions name places, attributes and nations by id; the culture themes and UI string tables from its `ui` section and each country's `culture` block: the neutral theme plus one per country, 324 UI strings in English of which 28 flavour labels are translated per culture language (tested against the source: `UiStringsTests`); the translation section (tongues with their glyph tables, scripts with their direction and font chains, the translator packs and their prices, the flip timing, the key-word rule, the fallback cipher, the notice) and every place's tongue, writing the 4 Speech translator upgrades and the notice trigger); the agency block (`agency`: the agency's printed name "TEMPORAL CUSTOMS", its programme line "Debt Relief Departures" and day 1's date, 14 Mar 2150, and the clerk's own account, `agency.clerk`: Citizen ID 773-2840-19, name, birth date, lineage, employment and note; the displaced's ranges; the accounts' ranges per status and the six transponder models, their status and class names checked); the neutral present (`present`: its facts, one per category the places author, each within a book row and shared with no place, its Culture from its wardrobe, its labels clashing with no place's) and the 2150 names (no name on both merged Future lists); and wires `ContentLibrary_Main`, each kind's case blueprint (`content.blueprints`, a blueprint of that kind, its archetype pool) and the day plans named in its `content` section, with each day's kinds (`days[].kinds`: known kinds with a blueprint, weights of 0 or more, one positive, and the displaced wherever premades may stand); checks every reference before writing anything, and that every day plan has a non-blank asset name used once, a day from 1 planned once and a queue of at least 1 (`DayPlans.Problems`, the validator's rule, in its words), the travellers' age range holds 1 ≤ `travellerAgeMin` ≤ `travellerAgeMax`, the agency block has a name, a programme line and a first date that is a calendar day of the common era (`AgencyContent.Problems`, the validator's rule), the clerk has a Citizen ID, a name and an employment (`ClerkContent.Problems`) (rules tested: `DayPlansTests`, `BirthDatesTests`, `AgencyCalendarTests`, `AccountTests`), every line id is unique, authored interview text is ASCII, no line is longer than the transcript holds, every spoken request has a unique id, a label, a prompt and a reply, no menu is fuller than the traveller wheel shows (document requests count only documents handed over on request; the hub counts the spoken requests, the look entry, and premade-bound dialogs once), no dialog effect carries a timed modifier or `SetFact`, every place fact fits a book row (28 characters), history text is ASCII, history values stay unique (tested: `HistoryChecksTests`) and fit the transcript lines, one Future era with one place per country, every condition reference resolves, every theme passes the contrast check (text 4.5:1, large text, window glyphs and hints 3:1, text fills at least 70% opaque, hover rings at least 9:1 apart, the evidence texts on each culture's windows and highlight) with a colour for every role, every culture table holds only flavour keys with the English placeholders and shapeable Arabic, and the keys the code derives from `ClueLabels.Key`, `Discrepancy.ReportKeyFor` and `Looks.SlotKey` exist (rules tested: `ContrastTests`, `PaletteTests`, `UiStringsTests`, `CultureChoiceTests`), the translation content is sound (`TranslationSettings.Problems`: tongue and pack ids, names and scripts, every pack used, every place's tongue known, 26 distinct cells per table with shapeable cells for Arabic, an ASCII fallback cipher, flip knobs at least 0, the key-word rule's slots ones a line fills and its words non-blank and listed once (`KeyWords.Problems`); prices at least 0, no hand-authored upgrade with a translator's id, an ASCII notice, the UI strings it reads) (rules tested: `TranslationTests`, `PseudoscriptTests`, `KeyWordsTests`), and the character data is sound (every wardrobe, label (at most 24 characters, unambiguous per gender), item art nation (a key token), Culture value (at most 28 characters, unique), weight, premade (name in no place's list, birth date inside the claimed place's birth years, record note, intro, neither claiming nor coming from a Future place) and day pool or forced slot; no Culture question); idempotent; owns only `Assets/Data/World/{Eras,Nations,Places,Rules,Interview,History,Premades,Culture,Translation}` (unlisted assets there go to the trash), creates a text-free placeholder wallpaper per culture (and the neutral one) only where the file is missing, never replacing art, and never removes hand-authored effects, triggers or upgrades (the library's triggers, effects and upgrades are rewired: hand-authored ones kept in order, generated unlock triggers, the translation notice, history-rule triggers, history-rule effects and leader effects appended, and the Speech translators after the hand-authored upgrades, in pack order; piece 9's Papers translators are no longer listed, so the owned folder's pruning trashes them); the scratchpad script that once wrote the source is retired; the source can also be written from the content spreadsheet (next line)
- [ ] Generate World also writes every place's `moment` (Chronopedia's article reads it) and the PC block, `world_source.json` `pc`, into `ContentLibrary_Main` (`ContentLibrarySO.Pc`): the Internet's sites (`id`, `kind` News/History/Ancestry/Static, `name`, `domain`, `glyph`, `blurb`, `fromDay`), the Static sites' authored pages (`site`, `path`, `title`, blocks of Headline, Heading, Paragraph, Note, Link and Box) and the Lineage Archive (`includePremades`, `people`, `relations`), and Mail's authored messages (`pc.mail`: each message's `id`, `fromDay`, `untilDay` (0 keeps it for the run), `flag`, `from`, `subject` and `body` paragraphs; an id used once, a first day from 1, a last day of 0 or not before it, a sender, a subject and a body: `Mailbox.AuthoredProblems`, tested: `MailboxTests`); it checks them before writing anything, and Validate Content Library checks the library the same way: unique site ids and domains (lower-case letters, digits, dots and dashes; never `home`, the start page's), a name, a first day of 1 or more, a front page for every Static site, authored pages only for Static sites with one page per path and an address on every Link; unique person ids (never a premade's), a name that is no place's given name and no premade's (a card must never describe someone at the desk), a known place, relations between known cards with a kind; and every `site.*` UI string the pages write with (`Sites.WordKeys`, 63 Full-tier strings) exists (rules tested: `SitesTests`, `AncestryPagesTests`)
- [ ] Content sheets (`docs/CONTENT_SHEETS.md`): the content can be authored in a spreadsheet. `Tools > TimeDesk > Export Content Spreadsheet` writes today's `world_source.json` to `ContentSheets/TimeDesk_Content.xlsx` (a README sheet listing every sheet and column with its type, what a blank cell means and its rules, then one sheet per table, 69 today: `places`, `placeFacts`, `wardrobe`, `premades`, `dialogs`, `dialogNodes`, `dialogLines`, `dialogChoices`, `questions`, `historyRules`, the Internet's `pcSites` and `pcPeople`, Mail's `pcMail` and the rest; one row per record, one column per field, a child sheet's first columns naming its parent row, lists as `a|b` in one cell), `Export Content Sheets (CSV)` the same as one UTF-8 CSV per sheet in `ContentSheets/csv/`, and `Write Content Spreadsheet Template` the same sheets with two example rows per table (`ContentSheets/TimeDesk_Content_Template.xlsx`); `Import Content Spreadsheet` (or `Import Content Sheets (CSV)`) writes the sheets back into `world_source.json` in its layout and line endings, then runs Generate World. The .xlsx is read and written with no third-party package (a zip of XML). One declarative map (`ContentSheetMap`: a sheet per table, a column per field, the types and rules as data) drives the export, the import, the README and the template, so a new source section is a map entry; export then import gives the source back byte for byte, straight, through CSV and through the workbook, and every key of the source must be mapped (tested: `ContentSheetMapTests`, `ContentSheetsTests`, `XlsxTests`, `RowTableTests`, `ContentJsonTests`). The import writes nothing when a check fails, each error naming its sheet, row and column: an unknown or missing column, a missing or unknown sheet, a cell of the wrong type, a blank required cell, a value outside its allowed set, a duplicate row name, a child row whose parent row does not exist, a name no row of the referred sheet has, a one-row sheet with more rows; a blank cell takes its column's documented default (tested: `ContentSheetsTests`); it also refuses while `world_source.json` holds content the map does not cover, so it never drops a section (the check tested: `ContentSheetsTests`)
- [ ] Content validator checks every place (five facts plus the Culture fact, which must equal its wardrobe's and be unique; names; birth years set; year; both genders' looks and their items' art nations (key tokens); look weights; a warning for a whole-outfit signature, which can never leak, except on Future places, whose culture-shaped outfit is their signature), wardrobe labels (unambiguous per gender), the look rules, every premade (id, name, birth date in range, true place, record note) and every day plan (a non-blank asset name used once, a day from 1 planned once and a queue of at least 1, the rule Generate World checks its source with (tested: `DayPlansTests`); today has places; every weighted era has one; every rule can be broken; premades, pooled and forced, claim and come from today's places; forced slots within the queue, a premade forced once, a warning for a forced premade in the first half of a day with rules; dress tells need a Costume Guide; at least one tell channel); no Culture question; questions (a book or the Citizen Record proves every question's category; one question per category; answer templates hold `{value}`), dialogs (structure, reachable endings, effects and their op types, one-shot), the interview's wording (the look label included), spoken requests (an id each, unique, a label, a prompt and a reply) and menu capacity, every upgrade id, and small talk for the eras day plans use; the Future (at most one Future era with a place per nation, leader effects broadcasting their culture cue, no baselines on Future places, day plans checked once per possible leader, a Future day allows every country, no nation+era rule on the Future, no premade claiming or coming from the Future), history values unique (tested: `HistoryChecksTests`), `SetFact` only in one-shot history-rule triggers (never a dialog, slot, upgrade, tier or leader effect), every trigger condition names what its type needs, effect op references, and ending fields (an attribute ending needs a day ending to replace), and the culture themes (the neutral theme, the reading table and the Latin fallback font exist; a theme per nation, or a warning; every role coloured, no culture colouring a diegetic role; the same contrast check as Generate World; flavour-only culture tables with matching placeholders; an OS font named where the labels need one; a missing wallpaper is a warning), and the translation (the same content checks as Generate World over the library and every place's tongue, every pack's Speech translator among the upgrades, the notice trigger when foreign speech starts after day 1; a library without translation data is an error), the agency block (the same checks as Generate World: `AgencyContent.Problems`, the displaced's day ranges included; tested: `AgencyCalendarTests`), the traveller kinds (every listed form's number is "TC-" and three digits and belongs to one form; a form handed over on request is askable by its blueprint's kind; a claim line for every kind a blueprint makes; every day plan has a kind with a blueprint and a positive weight, and the displaced wherever premades may stand; the present has a fact for every book; no 2150 name on both merged lists), and the desk fit (the checks Build Office UI runs, over `Desk_Default`: `ContentLibraryValidator.DeskFitProblems`)
- [ ] `Tools > TimeDesk > Build Office UI` — idempotent builder of the gameplay layer `Assets/Scenes/OfficeGameplay.unity` only (it opens it alone, creating it the first time, refuses in play mode or with unsaved scenes, never opens the art office, and keeps the build settings in boot order: `TitleScene` first, so a player build boots the title, then `OfficeScene`, `OfficeGameplay` and `HomeScene`, each enabled, with every other scene (the legacy `Test_DayLoop` prototype) listed after them, disabled (order tested: `BuildScenesTests`)); it adds the `Interactable` and `PCDesktop` layers, the scene contract asset and the gameplay materials; it stamps a `ThemeTag` (role, label key, style, shrink-to-fit) on every UI graphic it makes and on the existing objects of those names (which otherwise keep their properties), and logs an error for any untagged graphic on the desktop or overlay canvas; it builds the paper's printing parts (the text template every printed word clones, in the paper's text material: the font's, a shade heavier by the form style's ink weight, so small print reads at 720p as drawn; the fills and lines meshes in the vertex-coloured form materials, the faint seal with its placeholder ring and the box highlight) and the form style (`Assets/Data/Forms/FormStyle_Agency.asset`, created when missing: the forms' sizes in page heights and their colours), the examine material, the desk catcher and examiner, the frame's second hole, the office case HUD, the stamp tray, the verdict line and citation slip on the office overlay, the bubble's input above the wheel, and the desk view (its Cinemachine camera, inactive until the office binder poses it, the mat's click box and the "▲ Back" control with its hover hint); it logs an error for any text on the two canvases below its contrast minimum on what it is drawn on in the neutral theme or any culture's (`UiContrastCheck`: the graphics drawn before it under its centre, their theme colours, the wallpaper and sprites by their pixels; the worse of black and white behind a translucent stack; the size class from the text's smallest size as drawn at 1080p); it logs an error for a keyed label whose key the reading table lacks and for a graphic in a retired theme role; it builds the six desktop icons (their layer, the context menu, the Start menu's app entries and `DesktopApps` with each app's window) and logs an error for an app with no window; it reports a traveller wheel that fits fewer choices than the content's menu capacity, a desk with fewer paper spawn slots than a traveller's papers and a document template whose form does not place each field once, sets its own number or title, breaks onto a second page or does not fit the paper at its fields' longest values (`FieldLengths`: a place fact or a name at 28 characters, an origin at the content's longest origin label, a birth date at the widest date, the agency's numbers and dates at their makers' widths; the value floor, at most two lines; `DocumentForm.Problems` over `FormLayout.Check`, measured with TextMeshPro; tested: `FieldLengthsTests`) (the checks Validate Content Library runs too, `ContentLibraryValidator.DeskFitProblems`, audit R6-021); a paper whose size is not the form style's page aspect, and a form colour pair below its minimum: ink, labels and fine print on the paper and on a box, section heads on their band, and the ink on the hover tint and on every theme's pick highlight over a box at 4.5:1, rules and the stamp dash at 3:1 (`FormContrast`; tested: `FormContrastTests`); the desktop is a 1440×1080 World Space canvas on its own layer with the frame and clone cameras; the office-overlay canvas (the newsletters, the PC frame, the wheel, the callouts) scales from 1920×1080 and never below it (a screen wider than 16:9 gets extra width, a narrower one extra height), so the wheel shows every choice it fits at the reference size on any screen
- [ ] Travel rules: era / nation / nation+era forbidden, shown in briefing + directives; era and nation rules also apply to the day's Future place; a nation+era rule may not name the Future (validator)
- [ ] Day events system (before/after-case scheduled events; no event types authored yet; events placed past closing time never run)
- [ ] Debug panel (dev tools; the editor and development builds only, toggled with ~; closed, it draws nothing and costs nothing per frame; a click's cheat applies once the panel has drawn, checked in Unity): the Timeline Inspector and the state dump show the history (leader and since when, ranking, fact edits, pending carries), and the inspector the present culture, theme, label language, font, wallet word and missing UI strings; "Force leader" (one button per nation, or "No leader") sets the leader and its cue at once and keeps it every night of the session (not saved); the Future follows at the next office day; "Skip Day" sleeps (the day-boundary endings, then the nightly resolve and the next day)
