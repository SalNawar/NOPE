# Parked drafts (not implemented yet)

Saved 2026-09-25 so nothing lives only in a temp folder. These are REVIEWED DRAFTS,
not built features. Each becomes a real spec under `docs/superpowers/specs/` when its
piece is built (the build re-checks it against the code first).

- `specs/` — design specs for roadmap pieces 4 (characters), 5 (history changes facts)
  and 6 (the PC/UI reacts to history). Each was reviewed by two independent lenses.
- `decisions/` — the decision records behind them (made by Claude under Saleh's
  "go with all pieces, don't stop", open to his review), including the parked art-sync
  plan (`artsync_decisions.md`: adopting Codex's hybrid 3D office later).
- `art/` — the ChatGPT character/UI art brief v2 (to be reconciled with piece 4's final
  layer names before it is sent), the ready-to-paste ChatGPT message, the UI art rules,
  `coverage.json` (every required art file), the figure guide, and the scripts that
  rebuild them.
- `piece6-support/` — the contrast checker, Arabic shaper and draft culture/string
  tables used to validate the piece-6 spec.
