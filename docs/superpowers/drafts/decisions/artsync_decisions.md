# Art sync (after piece 3, before piece 4): decisions (Claude, under "go with all pieces dont stop", 2026-09-24)

Source analysis: SCRATCH/integration_map.json (decisions D1..D15 in `synthesis.decisions`; builder clash details in the maps). FACT: Codex already committed and pushed its art: b7f8671 is on origin/main (pushed without PR) and 8928865 on origin/art (checked out in E:\unity\NOPE, where Codex is still working: pass 33+, Unity + codex.exe running). The only merge conflict between my stack and art is Assets/Scenes/OfficeScene.unity (~225 hunks). Codex's office is a hybrid 3D scene (perspective Main Camera FOV 55 on renderer index 1 = OfficeForwardRenderer, HybridOffice tree with Blender booth/hall/city, 3D lights, SSAO, old 2D booth sprites inactive, CRT/READY clicks as collider-only proxies on 3D props, OfficeReadouts rewired to TMP texts on 3D props, a digital 3D clock, desktop/newsletter UI reskinned with sprites). Codex's docs: docs/ART_FINISH_PLAN.md, SCENE_CONTRACT.md, UNITY_INTEGRATION.md (read them).

Saleh's instruction: at the end, "commit and merge your work to main with the art work" and "push it too".

- D1 = A. Codex's hybrid 3D OfficeScene is the base. Codex (the scene) owns the world layout, 3D composition, renderer/volume/quality settings; my builder owns UI + gameplay wiring and must not clobber the world.
- D2 = B. Early sync now (piece 3 → 4 boundary) so pieces 4-6 are built against the real scene; a final re-sync happens at the end.
- D3 = B. Builder "existing-wins" everywhere, no mode flag: position, sprite, lens, projection, camera/vcam framing, booth layout are set only when an object is CREATED; readouts and clock are wired through existing references or named anchors; placeholders are created only when nothing exists. The scene still converges from empty. Remove only code that becomes truly unreachable. UI geometry stays authoritative where the builder owns it (per piece-6 intent), but the builder never destroys/recreates windows or buttons that already exist with Codex skins (stop the DestroyChildIfPresent pattern for those; update in place).
- D4 = C. Keep Codex's text-free UI skins (window glyph buttons, taskbar, intercom panel, citation slip, newspaper papers): the builder stops destroying or recolouring them (a "skinned" role later tinted white by piece 6's theme applier). Sprites with baked WORDS (Accept, Deny, Start) are replaced by builder labels over text-free backgrounds so colour + language can follow history (piece 6); check whether temporal_times_paper / shift_ledger_paper bake a masthead that would double the builder's text.
- D5 = A. Digital clock: wire Codex's 3D digital clock (ShiftClockDisplay or whatever its TMP is) through ShiftClockReadouts; retire the analog WallClock build, its hands and placeholder PNGs (and HandAngles if it loses its last caller; keep its tests only if it keeps a caller). The tray clock is unchanged. Update FEATURES.
- D6 = A (for piece 4, recorded here): the booth traveller is a billboard at an anchor `HybridOffice/Booth/TravellerAnchor` (behind the desk, near Codex's (0,1,2)); created once by the builder at a default pose, owned by the scene after; world-height knob in an SO (~1.7 m); unlit under the Forward renderer; fall back to B (hidden, Visitor window only) if screenshots show a mismatch. The art sync only creates the anchor if cheap; piece 4 places the figure.
- D7 = A. HoverHighlighter skips the sprite outline when the target SpriteRenderer is disabled (hand cursor still shows); record the gap in FEATURES and report it (a true 3D outline is a follow-up).
- D8 = A. Stability colour: tint the live StabilityPercent text by band (thresholds stay serialized knobs); optionally move the three-band choice into Domain (StabilityBand) with a decision-table test.
- D9 = A. Don't rewrite or untrack Codex's pushed churn (.agents/, csproj, TMP glyph cache, Screenshots.meta); mention it to Saleh as optional cleanup.
- D10 = A. Codex commits its own WIP; the sync merges what is pushed on origin/art (fetch first). Never commit files in E:\unity\NOPE.
- D11 = A (final step): fast-forward main to the verified integration branch and push directly, after committing a gate record in docs/reviews/.
- D12 = A. Leave Codex's `art` branch/checkout alone; report that main moved.
- D13 = A. ReactivePoster stays inactive; FEATURES updated; poster anchor + Codex's 27 posters mapping = follow-up (booth rework).
- D14 = A. Code keeps readySign/ReadyGate names; the displayed caption is a game-owned string "NEXT" on Codex's NextLabel (piece 6 localizes it); FEATURES says "READY/NEXT sign".
- D15: don't delete the remote `Art` branch; raise it with Saleh.

## Verification the art sync must pass (in the worktree's Unity)
- Import-only run succeeds (Codex's assets import in the worktree; note time).
- Codex-preservation dump: before/after running Build Office UI on the merged scene, every Codex-owned object keeps its transform, active state, sprite/material refs, camera projection/FOV/renderer index, vcam framing, colliders' positions; builder-owned wiring present (driver, readouts wired to Codex's 3D texts, desktop windows, intercom, compare bar, transcript/interview from piece 3). Build Office UI run twice = no change the second time.
- EditMode suite green (except the known UnitySkills failure); Generate World idempotent; validator clean.
- Scripted day-1/2 play-through on the hybrid scene (click the CRT/NEXT proxies, interview questions from piece 3, liar caught, closing), screenshots of the office view and the monitor view saved to the scratchpad.
